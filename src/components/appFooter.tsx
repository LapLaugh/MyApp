import * as React from 'react'
import BaseComponent from 'src/utils/baseComponent';
import classnames from 'classnames';

class AppFooter extends BaseComponent<any> {
    render() {
        return (
            <div className={classnames()}>

            </div>
        )
    }
}

export default AppFooter